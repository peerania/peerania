#!/usr/bin/env bash
set -ex

EOS_NODE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

cleos set contract peeranha.dev $EOS_NODE_DIR/../src/contracts/peeranha -p peeranha.dev
cleos set contract peeranha.tkn $EOS_NODE_DIR/../src/contracts/peeranha.tkn -p peeranha.tkn